Modify the way how configurable product is rendered in the shopping cart by modifying its item renderer.
Show in the line item a list of all simple product names that are available to the configurable.
